export enum Scenes {
  Main = "Main",
}

export enum Maps {
  Main = "Main",
}

export enum Animations {
  Player = "Soldier",
  Coin = "Coin",
}
export enum Sprites {
  Golem,
}

export enum Assets {
  MainAtlas = "MainAtlas",
  Tileset = "Tileset",
}

export enum Directions {
  UP = 0,
  DOWN = 1,
  LEFT = 2,
  RIGHT = 3,
}

export const TILE_HEIGHT = 32;
export const TILE_WIDTH = 32;
